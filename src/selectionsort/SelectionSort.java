package selectionsort;


import basis.Person;
import basis.PersonenSort;

public class SelectionSort implements PersonenSort {

	/**
	 * Sortier-Funktion Demo
	 */
	public void sort(Person[] personen) {
		// TODO: Your implementation
/*
		if (personen[0].compareTo(personen[1]) > 0) {
			Element an personen[0] ist größer als Element an personen[1]
		}*/


	}
	
}
